# 할 일 (체크리스트)

끝난 것은 지웁니다. 새로 생각난 것은 «아이디어 보관함» 에 적어 두고 나중에 의논합니다.

---

## 끝난 것

- [x] 깃허브로 옮기기, 파일 기능별로 나누기
- [x] 계정 로그인 (학생=학번 / 교사=이름) + RLS 로 자료 잠그기
- [x] GitHub Pages 로 서비스 — https://08hwichemi.github.io/interview-on/
- [x] 새 버전 알림 띠 (`version.txt` 와 `BUILD_ID` 를 견줍니다)
- [x] 디자인 통일 — 먹빛·종이빛, Pretendard
- [x] 관리자 화면과 교사 화면 분리
- [x] 관리자: 명단 등록(엑셀·붙여넣기), 비밀번호 초기화, 계정 삭제
- [x] 교사: 학생 고르기 → 질문 정하기 → 면접 진행 → 채점표
- [x] 평가를 세 층으로 — 한 줄 판정(우수·보통·미흡) · 4축 태그(내용·근거·말하기·태도) · 수기 요지
- [x] 시간 두 개 — 면접 전체 + 질문별
- [x] 리포트를 확인한 뒤 «학생에게 전달». 전달 뒤 고치면 «고친 날»이 남습니다
- [x] 누가기록 — 교사는 지난 회차를 눌러 다시 열고, 학생은 «내 면접 리포트» 에 회차가 쌓입니다
- [x] 첫인사(자기소개·지원동기)와 끝인사(마지막으로 하고 싶은 말)를 붙박이로
- [x] 리포트 인쇄 — 선생님은 종이로, 학생은 인쇄창에서 «PDF로 저장».
      질문 10개까지는 자동으로 줄여 A4 한 장에 담습니다
- [x] 잘못 시작한 면접 지우기 (교사 화면 리포트 아래 «이 리포트 지우기»)
- [x] 담당 학생 별표 — 명단 위에 모아 두고 바로 고릅니다.
      계정에 붙여 두어서(`public.teacher_favorites`) 다른 컴퓨터에서도 같습니다
- [x] 시계 하나로 — 진행 화면에서 시작, 한 단추로 두 시간이 같이 멈춤
- [x] 관리자는 로그인하면 바로 명단 관리 화면으로
- [x] 학생 앱: 안 읽은 리포트 빨간 숫자 + 실시간 알림 (`public.report_reads`)
- [x] 교사 화면 머리말 메뉴 — 면접 | 실전 면접 후기 | 대학별 기출 질문
- [x] 톡 — 학생과 선생님의 1:1 대화 (`public.chats`).
      이동수업 출석부 앱과 같은 짜임입니다 — 머리말 「💬 톡」 → 톡 목록 → 대화방 창.
      방 번호는 두 사람 아이디를 정렬해 붙여 만들어, 누가 먼저 걸든 같은 방입니다.
      리포트의 「선생님께 질문하기」도 그 선생님과의 같은 방으로 들어갑니다

---

## 지금 하는 것

- [ ] **생기부 PDF 에서 질문 뽑기**
  - 생기부는 브라우저에서만 읽고 버립니다. 서버에 원문을 올리지 않습니다
  - 창의적 체험활동상황 · 세부능력 및 특기사항 · 행동특성 및 종합의견 을 학년별로
  - 규칙 기반으로 시작. AI 는 나중에 선택지로
  - 뽑은 질문 옆에 원문 문장을 같이 보여주고, 전부 고칠 수 있게

---

## 다음

- [ ] 면접 도중 창이 닫혔을 때 **이어서 하기** (`status='진행중'` 인 면접을 찾아 되돌리기)
- [ ] 평가 단추 문구 다듬기 — 실제로 면접 보면서 자주 쓰는 말로
- [ ] 학교 설정 화면 (초기 비밀번호 값 등)

---

## 아이디어 보관함

### 학생 폰에 면접 시간 띄우기 ★ 먼저 해 볼 것

> 학생이 면접 전에 휴대폰으로 로그인해 두고, 교사가 진행 화면에서 «면접 시작» 을
> 누르면 학생 휴대폰에 시간이 뜨는 것. 학생도 시간을 보면서 면접을 봅니다.
> 화면에는 **면접 전체 시간을 크게, 질문별 답변 시간을 그보다 작게** 둘 다.

**됩니다.** 아래 녹음보다 훨씬 쉽습니다. 정해야 할 것(저장 용량·동의·삭제)이 없고,
보내는 것이 숫자 두 개뿐이라 먼저 해 볼 만합니다.

**모자란 조각은 하나뿐입니다 — 지금 시간이 서버에 안 올라갑니다.**
교사 화면의 시계(`teacher-app.js` 의 `tickId`)는 브라우저 안에서만 돕니다.
`interviews` 에 흐르는 시간을 적어 두면 학생 폰이 그걸 읽습니다.

```sql
alter table public.interviews
  add column if not exists running_since  timestamptz,  -- 시계가 돈 순간 (멈추면 null)
  add column if not exists elapsed_seconds int not null default 0,  -- 멈출 때까지 쌓인 초
  add column if not exists current_seq     int;         -- 지금 몇 번 질문인지
```

- **초를 그대로 보내지 않습니다.** 1초마다 서버에 쓰면 낭비입니다.
  «언제부터 돌기 시작했는지» 만 적어 두고, 학생 폰이 지금 시각에서 빼서 셉니다.
  시작·일시정지·질문 넘김, 이 세 번만 서버에 씁니다
- 학생 폰은 **Realtime**(`postgres_changes`) 으로 받거나, 안 켜면 5초마다 물어봅니다
- 학생은 `status='진행중'` 인 자기 면접을 읽을 수 있어야 합니다.
  지금 RLS 는 `'전달됨'` 만 내려주므로 **정책을 하나 더 만들어야 합니다**
  (진행중일 때는 시간 관련 칸만 보이게)
- 학생 화면은 홈에 뜨는 띠 하나면 됩니다 — 「지금 면접 중입니다」 + 시간 두 개.
  질문 글자는 **보내지 않습니다.** 미리 보이면 면접이 아닙니다

**걸림돌**

- 학생 폰 화면이 꺼집니다 → `navigator.wakeLock` 으로 붙잡습니다 (아이폰 16.4+)
- 교사 브라우저가 꺼지면 `running_since` 가 켜진 채 남습니다 →
  면접을 이어서 열 때 정리합니다 (아래 «이어서 하기» 와 같이 다룹니다)

### 학생 폰 연동 — 면접 중 녹음

> 교사가 학생 면접을 시작하면, 학생이 휴대폰을 켜 두었을 때 화면에 면접 시간이 뜨고
> 그 자리에서 녹음이 되게 하는 것.

**됩니다.** 필요한 조각이 이미 거의 다 있습니다.

- 학생 앱에 **녹음 기능이 이미 있습니다** (`assets/js/interview.js` 의 `MediaRecorder`)
- 면접이 시작되면 `interviews` 에 `status='진행중'` 인 줄이 생깁니다.
  학생 폰은 자기 면접이 진행중인지 알 수 있습니다
- 실시간으로 띄우려면 Supabase **Realtime**(`postgres_changes`) 을 켜면 됩니다.
  안 켜면 몇 초마다 물어보는 방식으로도 됩니다

**먼저 정해야 할 것**

- 녹음 파일을 **어디에 둘지** — Supabase Storage. 용량과 보관 기간을 정해야 합니다
  (한 명 10분이면 대략 5~10MB. 전교생 192명이면 1~2GB)
- **누가 들을 수 있는지** — 본인만? 면접한 선생님도?
- **언제 지울지** — 학년 끝나면 자동 삭제할지
- **동의** — 목소리는 개인정보입니다. 학생이 켤지 말지 고르게 해야 합니다

**걸림돌 (학생 앱에 이미 겪어서 경고 문구가 붙어 있는 것들)**

- 화면이 꺼지면 녹음이 끊깁니다
- 다른 앱으로 나가면 마이크가 끊길 수 있습니다
- 아이폰은 특히 까다롭습니다

→ 학생 리포트와 생기부 PDF 를 끝낸 뒤에 다시 의논합니다.

---

## 작업할 때 주의

- **로그인 규칙을 고칠 때**는 [계정-구조.md](계정-구조.md) 를 먼저 읽으세요.
  아이디→내부주소 변환이 앱과 서버 **두 곳**에 있어서 한쪽만 고치면 모든 로그인이 깨집니다
- **초기 비밀번호를 바꿀 때**는 두 곳을 같이 고칩니다 —
  Edge Function 의 `INITIAL_PASSWORD`, `assets/js/admin-roster.js` 의 `INITIAL_PW`
- **새 판을 올릴 때**는 `assets/js/update-bar.js` 의 `BUILD_ID` 와 `version.txt` 를 같은 값으로
- **`type="module"` 로 바꾸지 마세요.** 버튼들이 `onclick="함수명()"` 으로 부르고 있어서 전부 죽습니다
- **경로는 상대 경로로.** GitHub Pages 가 하위 경로에서 서비스합니다
- 참고 사이트(모두의진학)는 **구조만 봅니다.** 라이선스가 없어 문구와 코드는 가져오지 않습니다
