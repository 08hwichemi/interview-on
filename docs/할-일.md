# 할 일 (체크리스트)

끝난 것은 지웁니다. 새로 생각난 것은 «아이디어 보관함» 에 적어 두고 나중에 의논합니다.

---

## 끝난 것

- [x] 깃허브로 옮기기, 파일 기능별로 나누기
- [x] 계정 로그인 (학생=학번 / 교사=이름) + RLS 로 자료 잠그기
- [x] GitHub Pages 로 서비스 — https://08hwichemi.github.io/interview-on/
- [x] 새 버전 알림 띠 (`version.txt` 와 `BUILD_ID` 를 견줍니다)
- [x] 디자인 통일 — 먹빛·종이빛, Pretendard
- [x] 관리자 화면과 교사 화면 분리
- [x] 관리자: 명단 등록(엑셀·붙여넣기), 비밀번호 초기화, 계정 삭제
- [x] 교사: 학생 고르기 → 질문 정하기 → 면접 진행 → 채점표 → 공개

---

## 지금 하는 것

- [ ] **학생 리포트 화면** — 지금은 면접을 끝내도 학생 앱에 읽을 화면이 없습니다
  - 회차 목록 → 회차 하나 열기
  - 채점표(항목별 A~E), 총평, 질문별 시간과 눌린 태그

---

## 다음

- [ ] **생기부 PDF 에서 질문 뽑기**
  - 생기부는 브라우저에서만 읽고 버립니다. 서버에 원문을 올리지 않습니다
  - 규칙 기반으로 시작. AI 는 나중에 선택지로
- [ ] 면접 도중 창이 닫혔을 때 **이어서 하기** (`status='진행중'` 인 면접을 찾아 되돌리기)
- [ ] 평가 단추 문구 다듬기 — 실제로 면접 보면서 자주 쓰는 말로
- [ ] 학교 설정 화면 (초기 비밀번호 값 등)

---

## 아이디어 보관함

### 학생 폰 연동 — 면접 중 녹음

> 교사가 학생 면접을 시작하면, 학생이 휴대폰을 켜 두었을 때 화면에 면접 시간이 뜨고
> 그 자리에서 녹음이 되게 하는 것.

**됩니다.** 필요한 조각이 이미 거의 다 있습니다.

- 학생 앱에 **녹음 기능이 이미 있습니다** (`assets/js/interview.js` 의 `MediaRecorder`)
- 면접이 시작되면 `interviews` 에 `status='진행중'` 인 줄이 생깁니다.
  학생 폰은 자기 면접이 진행중인지 알 수 있습니다
- 실시간으로 띄우려면 Supabase **Realtime**(`postgres_changes`) 을 켜면 됩니다.
  안 켜면 몇 초마다 물어보는 방식으로도 됩니다

**먼저 정해야 할 것**

- 녹음 파일을 **어디에 둘지** — Supabase Storage. 용량과 보관 기간을 정해야 합니다
  (한 명 10분이면 대략 5~10MB. 전교생 192명이면 1~2GB)
- **누가 들을 수 있는지** — 본인만? 면접한 선생님도?
- **언제 지울지** — 학년 끝나면 자동 삭제할지
- **동의** — 목소리는 개인정보입니다. 학생이 켤지 말지 고르게 해야 합니다

**걸림돌 (학생 앱에 이미 겪어서 경고 문구가 붙어 있는 것들)**

- 화면이 꺼지면 녹음이 끊깁니다
- 다른 앱으로 나가면 마이크가 끊길 수 있습니다
- 아이폰은 특히 까다롭습니다

→ 학생 리포트와 생기부 PDF 를 끝낸 뒤에 다시 의논합니다.

---

## 작업할 때 주의

- **로그인 규칙을 고칠 때**는 [계정-구조.md](계정-구조.md) 를 먼저 읽으세요.
  아이디→내부주소 변환이 앱과 서버 **두 곳**에 있어서 한쪽만 고치면 모든 로그인이 깨집니다
- **초기 비밀번호를 바꿀 때**는 두 곳을 같이 고칩니다 —
  Edge Function 의 `INITIAL_PASSWORD`, `assets/js/admin-roster.js` 의 `INITIAL_PW`
- **새 판을 올릴 때**는 `assets/js/update-bar.js` 의 `BUILD_ID` 와 `version.txt` 를 같은 값으로
- **`type="module"` 로 바꾸지 마세요.** 버튼들이 `onclick="함수명()"` 으로 부르고 있어서 전부 죽습니다
- **경로는 상대 경로로.** GitHub Pages 가 하위 경로에서 서비스합니다
- 참고 사이트(모두의진학)는 **구조만 봅니다.** 라이선스가 없어 문구와 코드는 가져오지 않습니다
